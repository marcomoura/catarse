%h1{:class=>"#{align_logo_when_home}"}
  = link_to root_path do
    = image_tag "sites/#{current_site.path}/logo.png", :title => t('.title')
  .slogan
    %strong= t("sites.#{current_site.path}.title")
    %span= t("sites.#{current_site.path}.subtitle")
